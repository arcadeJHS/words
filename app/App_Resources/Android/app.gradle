// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "jhs.nativescript.words"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
  project.ext {
    googlePlayServicesVersion = "11.2.+"
  }
}
